// estas funciones son de ejemplo

// export const imageType = (linda) => {
//   let nuevo = ""
//   pokemon.type.length > 1 ?
//   pokemon.type.forEach(pokemon => {
//     nuevo += `<p>${pokemon.type}</p>`: nuevo += `<p>${pokemon.type[0]}</p>`
//   })
// }

export const anotherExample = () => {
  return "OMG";
};
